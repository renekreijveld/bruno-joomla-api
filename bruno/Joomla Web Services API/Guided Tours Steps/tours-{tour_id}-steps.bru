meta {
  name: tours/{tour_id}/steps
  type: http
  seq: 1
}

get {
  url: {{base_url}}/v1/tours/{{tour_id}}/steps
  body: none
  auth: inherit
}

example {
  name: 200 Response
  description: todo: response object
  
  request: {
    url: {{base_url}}/v1/tours/{{tour_id}}/steps
    method: GET
    mode: none
  }
  
  response: {
    status: {
      code: 200
      text: OK
    }
  
    body: {
      type: text
      content: '''
  
      '''
    }
  }
}
