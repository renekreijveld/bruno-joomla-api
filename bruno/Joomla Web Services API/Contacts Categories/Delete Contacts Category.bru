meta {
  name: Delete Contacts Category
  type: http
  seq: 4
}

delete {
  url: {{base_url}}/v1/contacts/categories/{{category_id}}
  body: none
  auth: inherit
}

example {
  name: 200 Response
  description: todo: response object
  
  request: {
    url: {{base_url}}/v1/contacts/categories/{{category_id}}
    method: DELETE
    mode: none
  }
  
  response: {
    status: {
      code: 200
      text: OK
    }
  
    body: {
      type: text
      content: '''
  
      '''
    }
  }
}
