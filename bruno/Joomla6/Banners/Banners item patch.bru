meta {
  name: Banners
  type: http
  seq: 4
}

patch {
  url: {{base_url}}/v1/banners/{{banner_id}}
  body: json
  auth: inherit
}

body:json {
  {"state":1}
}

vars:pre-request {
  banner_id: 1
}

vars:post-response {
  : 
}

settings {
  encodeUrl: true
  timeout: 0
}
